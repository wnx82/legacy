{
  "name": "@legacy/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, DTO, validations, constantes, statuts et rôles partagés entre l'API et les frontends Legacy.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
